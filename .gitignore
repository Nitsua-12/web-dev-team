# Secrets
.env
**/.env

# Local databases (operational state, not source)
*.db
**/*.db
agents/website_demo/indexable_slugs.txt

# Python
.venv/
**/.venv/
__pycache__/
**/__pycache__/

# Node (wrangler via npx needs a nearby writable node_modules on some
# machines -- this is a local cache workaround, not a real dependency)
node_modules/
**/node_modules/

# Claude Code local-only settings
.claude/settings.local.json

# Per-run operational logs
logs/

# Git worktrees for isolated feature work
.worktrees/

# Playwright MCP tool output (screenshots/snapshots, not project source)
.playwright-mcp/
